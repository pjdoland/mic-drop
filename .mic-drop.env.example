# ---------------------------------------------------------------------------
# mic-drop — persistent configuration
#
# setup.sh generates .mic-drop.env from your answers during first run.
# Copy this file to .mic-drop.env and edit if you want to pre-configure
# without running the interactive prompt.
#
# DO NOT commit .mic-drop.env — paths are machine-specific and may contain
# sensitive API keys.
# ---------------------------------------------------------------------------

# Where Tortoise TTS stores its downloaded model weights (~2–4 GB).
# Leave blank to let Tortoise use its own default (~/.cache/tortoise-tts).
# Only needed when using --tts-engine tortoise (default).
TORTOISE_CACHE_DIR=

# OpenAI API key for OpenAI TTS (gpt-4o-mini-tts, tts-1, tts-1-hd).
# Required when using --tts-engine openai.
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=
